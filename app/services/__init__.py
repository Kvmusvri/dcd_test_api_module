"""Сервисный слой стенда: чистая логика без HTTP-слоя.

Роутеры (`app/routers/`) остаются тонкими — валидация запроса и формат
ответа; вся работа с картинками, внешними API и расчётами живёт здесь:
- `imaging` — PIL-утилиты (размеры, EXIF, кроп-под-клиента, сниффинг формата);
- `replicate_client` — транспорт к API Replicate (Files API + predictions);
- `colorimetry` — sRGB/Lab/ΔE2000;
- `car_roi` — v1-детект кузова компаратора (классический CV).
"""
